# Velero Credentials Template
# Copy this file and fill in your credentials, then use it during Velero installation

# AWS S3 Credentials
[default]
aws_access_key_id=YOUR_AWS_ACCESS_KEY_ID
aws_secret_access_key=YOUR_AWS_SECRET_ACCESS_KEY

# For MinIO (use AWS format)
# [default]
# aws_access_key_id=minioadmin
# aws_secret_access_key=minioadmin

# Azure Blob Storage (if using Azure)
# AZURE_STORAGE_ACCOUNT_ACCESS_KEY=your_access_key

# Google Cloud Storage (if using GCS)
# Use service account JSON key file instead

